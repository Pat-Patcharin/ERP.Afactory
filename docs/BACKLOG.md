# Backlog — งานที่รู้แล้วว่าค้าง

รายการนี้เคยอยู่ในบันทึกนอก repo ย้ายเข้ามาไว้ที่นี่เพราะคนที่มาอ่านโค้ดทีหลัง
ต้องเห็นด้วยว่าอะไรที่ "ยังไม่ได้ทำ" ไม่ใช่ "ทำพลาด"

ทุกข้อในนี้ถูกพิจารณาแล้วและ**ตั้งใจเลื่อน** ไม่ใช่ของที่มองข้าม
อย่าไล่แก้แถมตอนทำงานอื่น — เจ้าของโปรเจกต์ตรวจงานทีละขั้น
งานที่หลุดออกนอกขอบเขตของขั้นที่กำลังทำ ทำให้เขาตรวจไม่ได้

---

## กฎที่งานชุดนี้สอนซ้ำ ๆ — อ่านก่อนจะเพิ่มกฎใหม่

**กฎที่เขียนครบทุกบรรทัด แต่ไม่เคยปฏิเสธใครเลย อ่านเหมือนโค้ดที่ทำงานได้**

เกิดมาแล้วหลายรอบในโปรเจกต์นี้:

- `checkQuotedPrice()` เขียนเสร็จสมบูรณ์ แล้วไม่มีใครเรียก
- `maySignAt("manager")` ผ่านทุกบทบาทที่มีสิทธิ์ approve — ไม่มีใครถูกกั้น (ปิดแล้วที่ N1)
- `headerFromSource(...).billType` คืนค่าที่ไม่มีใครอ่าน (ลบแล้ว)
- สวิตช์ In-App ในหน้า Notification Settings เขียน flag ที่ตัวส่งไม่เคยอ่าน (ถอดแล้ว — ข้อ N-1)
- `isOverBilled()` กับ `remainingBillable()` อธิบายกฎ "วางบิลเกินที่ส่งไม่ได้" มาตั้งแต่ต้น
  แล้ว**ไม่มีใครเรียกทั้งคู่** ฟอร์มตั้งค่าเริ่มต้นให้สวย ๆ แล้วไม่มีใครตรวจว่าเซฟอะไรลงไป (ต่อสายแล้วที่ N8)
- `NOTIFICATIONS[].roles` เป็นรายชื่อที่พิมพ์ไว้ ขัดกับกฎข้อ 1 ของ `notify.ts` ที่คำนวณผู้รับจากสิทธิ์ (ข้อ N-1)
- ตอนนี้: `priceApproval()` ทำงานถูกทุกบรรทัด แต่หาแถวราคากลางไม่เจอสักบรรทัด (ข้อ 11)

**เวลาเพิ่มกฎ ต้องมีเทสต์ที่พิสูจน์ว่ามีเคสหนึ่งถูกปฏิเสธจริง
เทสต์ที่พิสูจน์ว่าเคสที่ผ่านนั้นผ่าน ไม่ได้พิสูจน์อะไรเลย**

---

## กฎถาวรสำหรับงาน refactor — ย้ายของเดิมก่อน แล้วค่อยปรับ

**ห้ามเขียนใหม่ให้เหมือน** ตอนดึงโค้ดร่วมออกมา ให้ย้ายบล็อกเดิมมาทั้งก้อนก่อน
แล้วค่อยแก้ทีละจุดโดยเห็น diff ถ้าพิมพ์ขึ้นใหม่จากความจำ มันจะ "เหมือน" ในสายตา
แต่ต่างในรายละเอียดที่สำคัญที่สุดเสมอ

**"เทสต์เขียว" กับ "ไม่เปลี่ยนพฤติกรรม" ไม่ใช่เรื่องเดียวกัน**

พิสูจน์แล้วที่ P1a — ดึงโครงร่วมของ SalesRequestEditor กับ QuotationEditor
เทสต์ 172 ตัวเขียวหมด แต่มีสองจุดที่พฤติกรรมเปลี่ยนไปแล้วและ**ไม่มีเทสต์ตัวไหนแตะเลย**

| จุด | ของจริง | ที่เขียนใหม่ | ผู้ใช้เจออะไร |
|---|---|---|---|
| `docMode` | `preview === "document"` | `preview ?` (หลวมกว่า) | เปิด print preview แล้วฟอร์มข้างล่างกลายเป็นโหมดอ่าน ปิด overlay มาแล้วแก้อะไรไม่ได้ |
| `cancel` | ลบร่างที่เก็บไว้ก่อนออกจากหน้า | ลืมลบ | ครั้งหน้าเข้ามา ระบบถามให้กู้คืนงานที่เพิ่งกดทิ้งไป |

ทั้งคู่ผู้ใช้เจอทันทีในการใช้งานปกติ แต่ชุดเทสต์ 1,561 ตัวไม่มีตัวไหนจับได้

**วิธีที่ใช้ได้จริงคือไล่เทียบกับของเดิมทีละบรรทัด ไม่ใช่เชื่อว่าเทสต์ครอบคลุมพอ**
`git show HEAD:<ไฟล์>` แล้ว diff กับของใหม่ คือขั้นตอนบังคับ ไม่ใช่ทางเลือก
เทสต์เป็นตาข่ายรับ ไม่ใช่หลักฐานว่าเท่ากัน

**ขั้นตอนนี้จับของได้แล้ว 3 ครั้ง ไม่ใช่ขั้นตอนที่ข้ามได้**

| รอบ | จับได้ | เทสต์จับไหม |
|---|---|---|
| P1a | `docMode` เขียนหลวมเกิน | ไม่ |
| P1a | `cancel` ไม่ลบร่างที่เก็บไว้ | ไม่ |
| P1b-1 | `applyProduct` เป็น arrow ใน config ทำให้ `pickProduct`/`addLines` เลิก stable | ไม่ |

ครั้งที่สามต่างจากสองครั้งแรก: **พฤติกรรมถูกทุกอย่าง แค่ทำงานเกินจำเป็น**
ตารางสินค้า re-render ฟรีทุกครั้งที่พิมพ์ เทสต์จับไม่ได้เพราะผลลัพธ์ถูก
เป็นของที่มีแต่การอ่าน diff เท่านั้นที่เห็น

อีกสองจุดในรอบเดียวกันที่เครื่องมือจับได้ (จึงไม่อันตรายเท่า):
`onGridKey` ที่ย่อเงื่อนไขผิด — เทสต์จับ 1 ตัว ·
`PasteItemsModal` ที่เขียนใหม่แล้ว props ไม่ตรงกับ `Modal` จริง — tsc จับ

**และกฎเดียวกันนี้ใช้กับตัวเลขทุกชนิด ไม่ใช่แค่โค้ด** ตอนเขียน backlog N-6
ผมใส่ค่า contrast ของ `#c2410c` เป็น 4.7:1 จากความจำ วัดจริงได้ 5.18:1
ตัวเลขที่จำมาผิดได้เท่า ๆ กับโค้ดที่จำมาผิด ให้วัดแล้วค่อยเขียน

---

## เทสต์ที่เขียนไว้ห้ามบางอย่าง ต้องฉีดของผิดเข้าไปดูก่อนว่าแดงจริง

**เทสต์ที่ผ่านตั้งแต่วันแรกยังไม่ได้พิสูจน์อะไร** มันอาจผ่านเพราะเงื่อนไข
ที่มันจับไม่เคยถูกทดสอบเลย

เกิดจริงที่ P2b — เขียนเทสต์ห้ามแถบซ้ายอ้าง token ของเอกสาร แล้วรัน mutation
เพื่อพิสูจน์ ปรากฏว่า regex ที่ใช้ฉีดของผิดไม่ match อะไรเลยสักบรรทัด
เทสต์จึง "ผ่าน" ทั้งที่ไม่เคยถูกทดสอบ ต้องฉีดโค้ดตรง ๆ เข้าไปใหม่ถึงเห็นว่าแดงจริง

> **เทสต์ผ่านเพราะไม่ได้ถูกทดสอบ ไม่ใช่เพราะมันดี**

ขั้นตอนบังคับสำหรับเทสต์แนวห้าม (grep, guard, permission, formula):

1. เขียนเทสต์ให้เขียว
2. **ใส่ของผิดเข้าไปจริง ๆ** แล้วยืนยันว่าแดง — และแดง**ตรงตัวที่คิดว่าจะจับ**
3. คืนค่าเดิม ยืนยันว่าเขียวกลับ
4. ถ้า mutation ไม่ทำให้อะไรแดง แปลว่าเทสต์ไม่ได้จับสิ่งที่คิด ไม่ใช่ว่าโค้ดดี

> **ยืนยัน mutation ด้วยการพิมพ์ค่าที่ได้ออกมาดูว่าเปลี่ยนจริง
> `grep` พิสูจน์แค่ว่าตัวอักษรอยู่ในไฟล์ ไม่ได้พิสูจน์ว่าบรรทัดนั้นถูกเรียก**

เขียนเทสต์เล็ก ๆ ที่ `console.log` ค่าที่ฟังก์ชันคืนออกมา รันหนึ่งครั้ง
เห็นค่าที่ผิดกับตาแล้วค่อยไปรันเทสต์จริง สามบรรทัดนี้ไม่ใช่ส่วนเกิน
มันคือส่วนที่ทำให้ขั้นตอนข้างบนมีความหมาย

**กฎเดิมตรงนี้เขียนว่า "ยืนยันว่า mutation ถูกใส่เข้าไปจริง (`grep -c` ก่อนรัน)"
ซึ่งเป็นกฎที่ไม่กัดเสียเอง** — ตรงกับรูปแบบที่เอกสารนี้เตือนเรื่องอื่นไว้ทั้งหน้า

เจอตอน N-9a: ฉีดโค้ดให้ `audit()` กลับไปเขียน พ.ศ. แล้ว `grep -c '543'` ตอบ 1
ตามที่กฎสั่ง จึงเชื่อว่าฉีดสำเร็จ เทสต์ตอบว่าผ่าน — ที่จริงตัวแทนที่กิน
เครื่องหมาย `\` ไป regex กลายเป็น `/(d{4})/` ซึ่งไม่ match อะไรเลย
543 อยู่ในไฟล์จริง **แต่อยู่ในโค้ดที่ไม่มีวันทำงาน**
`grep` ตอบถูกทุกอย่างและตอบคำถามผิดข้อ

เกิดซ้ำอีกสองครั้งในรอบเดียวกัน (`perl` พังเพราะ `$` · `node -e` กิน escape)
ทั้งสามครั้งเทสต์รายงานว่าผ่าน รวมเป็นเจ็ดครั้งในสามรอบงานที่ mutation
เป็นหมันโดยไม่มีใครรู้ ครั้งนี้ต่างตรงที่**กฎที่เขียนไว้กันเรื่องนี้เองก็ไม่กัด**

เมื่อไหร่ที่ต้องแก้ไฟล์เพื่อฉีดของผิด ให้ใช้ตัวแก้ไฟล์ที่เห็น diff
อย่าใช้ `sed` / `perl` / `node -e` กับสตริงที่มี `\` หรือ `$`

---

## กฎถาวร: ต่อสายฟิลด์ใหม่ ต้องตรวจสองรอบ — "เปิดกลับมาแล้วยังอยู่ไหม"

**เทสต์ที่ตรวจว่า "เซฟแล้วมีค่าในระเบียน" ไม่พอ ไม่เคยพอ** ต้องมีสองข้อเสมอ

1. เซฟแล้วระเบียนมีค่า
2. **เปิดเอกสารกลับมาแก้ แล้วค่ายังอยู่** — แล้วเซฟซ้ำ ยืนยันว่ารอดรอบสอง

ข้อสองต้องเดินผ่านทางเปิดจริง (`draftFrom*`) ไม่ใช่ประกอบ draft ขึ้นเอง

เหตุผลไม่ใช่ความละเอียด แต่เป็นเพราะข้อแรกเขียวได้บนโค้ดที่ทำค่าหาย
เจอจริงที่ A2a — `draftFromQuotation` จบด้วย `applyCustomer()` ซึ่งเขียนทับ
ที่อยู่ทั้งหกช่องด้วยค่าจากทะเบียนคู่ค้า **อ่านค่าจากระเบียนก่อนแล้วเรียกทีหลัง
= ไม่ได้เก็บอะไรเลย** และเทสต์แบบข้อแรกจะผ่านสนิท
A1 เจอรูปเดียวกันมาก่อนแล้วกับค่าหัวบิล — ถ้าไม่มีข้อสอง การเซฟครั้งที่สอง
จะล้างสิ่งที่ครั้งแรกเขียนไว้เงียบ ๆ

> ในตัวแก้ไขที่ดูดข้อมูลจากทะเบียนตอนโหลด **ให้ดูลำดับเสมอ**
> ทะเบียนลงก่อน ค่าของเอกสารทับทีหลัง

ใช้กับ**ทุกฟิลด์ที่ต่อสายใหม่ต่อจากนี้** ไม่ใช่แค่ที่เจอในรอบนั้น
(เจ้าของโปรเจกต์กำหนดเป็นกฎถาวร 2026-08-09)

---

## ข้อ backlog ที่พิสูจน์ด้วยโค้ดได้ ให้เขียนเทสต์ที่แดงเมื่อมันถูกแก้

backlog ส่วนใหญ่ตายเพราะไม่มีใครกลับไปอ่าน เทสต์ที่บันทึก**สภาพที่ผิดอยู่ตอนนี้**
จะแดงขึ้นมาเองในวันที่สภาพนั้นหายไป — คือเตือนตัวเองตอนหมดความจำเป็นพอดี

ทำแล้วสองข้อ

- **N-6** `expect(contrast("#f97316", WHITE)).toBeLessThan(3)` ใน `tests/doc-tokens.test.ts`
  แดงเมื่อฝั่งขายถูกแก้ให้ผ่านเกณฑ์
- **ข้อ 11** `tests/price-approval.test.ts` — แดงเมื่อรหัสสินค้าในเอกสาร
  เริ่มหาแถวราคากลางเจอ แปลว่าด่านราคาขั้นต่ำเริ่มกัดจริงแล้ว

เขียนคอมเมนต์กำกับให้ชัดว่า **เทสต์ตัวนี้แดง = ข่าวดี** ไม่ใช่ของพัง
ไม่งั้นคนถัดไปจะ "แก้" ให้มันเขียวกลับ

---

## คอมเมนต์ที่โค้ดไม่ได้ทำตาม — เจอ 4 ครั้งในรอบเดียว

คอมเมนต์อธิบายกฎที่กว้างกว่าโค้ดจริง ผู้อ่านเชื่อคอมเมนต์
เพราะมันอยู่ตรงนั้นเพื่อบอกว่าโค้ดทำอะไร ทั้งรอบ P1–N9 เจอสี่ครั้ง

| ที่ | คอมเมนต์อ้างว่า | โค้ดทำจริง | ผลที่ตามมา |
|---|---|---|---|
| `projected` | หักทั้ง reserved และ backOrder | หักตัวเดียว | ตัวเลขพร้อมขายสูงเกินจริง |
| `StockAdvicePanel` | `!productStock(code)` แปลว่าไม่เคยมีสต๊อก | ฟังก์ชันคืนศูนย์ให้ทุกรหัสในแคตตาล็อก | สาขานั้นไม่เคยทำงาน สินค้าที่ไม่มีสต๊อกโชว์เป็นแถวศูนย์ |
| `normaliseEra` | เดินดูข้อความทั้งหมด ไม่ไล่ตามชื่อฟิลด์ | ไล่ตามชื่อฟิลด์อยู่สี่ตัว | `approval.at` หลุด ใบเสนอราคาออกไปโดยหัวกระดาษ 2569 ลายเซ็น 2026 |
| `audit()` | "D4 จะจัดการให้ทั้งแอป" | D4 จบไปแล้วโดยไม่ได้แตะบรรทัดนี้ | เขียน พ.ศ. ลงบันทึกการใช้งานทุกครั้งที่มีคนทำอะไร |

ครั้งที่สามร้ายที่สุดในแง่ที่ไปถึงมือลูกค้า และเป็นครั้งแรกที่**มีเทสต์จับได้เอง** —
`tests/era.test.ts` เจอตั้งแต่รันแรกที่เขียนเสร็จ ก่อนที่ใครจะไปอ่านโค้ด

**นี่คือเหตุผลว่าทำไม tripwire ต้องมี** สองครั้งแรกจับได้เพราะบังเอิญไปอ่านเจอ
ซึ่งไม่ใช่วิธีที่ทำซ้ำได้ เทสต์ที่ตรวจ**ผลลัพธ์**แทนที่จะตรวจว่าโค้ดเรียกอะไร
เป็นตัวเดียวที่จับความต่างระหว่างคอมเมนต์กับโค้ดได้ เพราะมันไม่อ่านทั้งสองอย่าง

เวลาเขียนคอมเมนต์ที่อ้างว่า "ทั้งหมด" / "ทุกอัน" / "ไม่ไล่ตามชื่อ" ให้ถามว่า
มีเทสต์ที่จะแดงไหมถ้าโค้ดทำได้แคบกว่าที่เขียน ถ้าไม่มี ให้เขียนสิ่งที่โค้ดทำจริง

### คอมเมนต์ที่สัญญาว่า "ขั้นถัดไปจะจัดการ" อันตรายเป็นพิเศษ

ครั้งที่สี่ต่างจากสามครั้งแรก — **เราสร้างมันเองระหว่างทาง** ไม่ได้รับมรดกมา
ตอน D2 เขียนคอมเมนต์ว่าความต่างของศักราชในบรรทัดนี้ "D4 จะจัดการให้ทั้งแอป"
แล้ว D4 ก็มาถึง ตัดสินกฎ เขียนเอกสาร ตั้งทริปไวร์ และ**ไม่ได้กลับมาที่บรรทัดนี้**

สามอันแรกยังผิดแบบตรวจได้ — เอาโค้ดมาเทียบกับคอมเมนต์ก็เห็น
อันนี้ตรวจแบบนั้นไม่ได้ เพราะตอนเขียน**มันจริง** และกลายเป็นเท็จทีหลัง
โดยไม่มีใครแตะบรรทัดนั้นเลย ไม่มีขั้นตอนไหนในการทำงานที่จะพามาเจอ
คนที่มาอ่านทีหลังจะเห็นว่า "D4 จัดการแล้ว" แล้วเดินผ่านไป

**กติกา: ถ้าต้องเขียนคอมเมนต์ที่เลื่อนงานไปขั้นถัดไป ต้องผูกกับเทสต์ที่แดง
จนกว่าจะทำเสร็จ** ถ้าเขียนเทสต์แบบนั้นไม่ได้ แปลว่ายังไม่รู้ว่า "เสร็จ" หน้าตายังไง
ซึ่งแปลว่ายังไม่ควรเลื่อน คำสัญญาที่ไม่มีใครทวงคือคำสัญญาที่ไม่มีวันถูกทำ
ตรงกับกฎที่มีอยู่แล้วข้างบน — ข้อ backlog ที่พิสูจน์ด้วยโค้ดได้ ให้เขียนเทสต์ที่แดง

---

## เปิดอยู่ — เรียงตามความเร่ง

### 11. ราคาขั้นต่ำตรวจไม่ได้สักบรรทัด เพราะรหัสสินค้าคนละชุดกัน

**นี่คือข้อที่ต้องมาก่อนทุกข้อ** ด่านราคาคือเหตุผลที่ N1–N7 มีอยู่ และมันยิงไม่ออก

รหัสในเอกสารใช้ชุด `AA-TH003-WL`, `AT-SL001`, `AB-AC001`
ส่วนราคากลาง 807 แถวใช้ชุด `D-AD001-01`, `H-AD001-01` — คนละ namespace

วัดจากข้อมูลจริงในตอนนี้:

```
ใบเสนอราคา 7 ใบ · บรรทัดรวม 14 · หาราคากลางไม่เจอ 14 · ถูก flag 0
```

แปลว่าทุกใบออกมาเป็น `level: "admin"` โดยที่ระบบไม่ได้ตรวจราคาสักบรรทัด
ชั้นอนุมัติของผู้จัดการที่สร้างไว้ที่ N1 จึงไม่มีทางถูกเรียกใช้จากข้อมูลชุดนี้

**ผลพลอยเสียที่ต้องรู้: ด่าน `noCost` ก็ยิงไม่ออกด้วยเหตุผลเดียวกัน**
`lib/workflows-outbound.tsx:190` และ `:712` ปฏิเสธการ submit ถ้ามีบรรทัดที่ไม่มีต้นทุน
ซึ่งตรงกับกฎเหล็กใน `CLAUDE.md` ว่า `PENDING_COST` (56 แถว) ต้อง **บล็อกการขาย**
แต่บรรทัดที่หาแถวราคากลางไม่เจอจะตกลง `uncheckable` ไม่ใช่ `noCost`
เมื่อหาไม่เจอทั้ง 14 บรรทัด `noCost` จึงว่างเสมอ — **แถว `PENDING_COST` ทั้ง 56 แถว
เข้าไม่ถึงเส้นทางเอกสารเลย** ด่านที่ควรจะห้ามขายจึงไม่เคยห้ามใคร

**ห้ามปิดข้อนี้ด้วยการแก้ mock ให้รหัสตรงกัน** นั่นได้เดโมที่ดูถูกและยังพิสูจน์อะไรไม่ได้
ต้องแก้ตรงที่ import ข้อมูลจริง — ให้ product master, price master และเอกสาร
ใช้ product code space เดียวกัน และจะถือว่าแก้จริงก็ต่อเมื่อ
**ใบเสนอราคาที่ตั้งราคาต่ำกว่า `price_last` ถูกดันขึ้นไปหาผู้จัดการได้จริง**

### N-6. สีส้มของแบรนด์ไม่ผ่านเกณฑ์ contrast บนเอกสารที่ส่งถึงมือลูกค้า

พบตอนวัดโทนสำหรับ P2 — ไม่ใช่เรื่องความสะอาดของโค้ด แต่กระทบผู้ใช้และลูกค้าจริง

```
#f97316 บนพื้นขาว = 2.80:1
  เกณฑ์ WCAG AA ตัวอักษรปกติ  4.5:1   ← ไม่ผ่าน
  เกณฑ์ตัวอักษรขนาดใหญ่        3.0:1   ← ไม่ผ่านเช่นกัน
```

จุดที่โดน — อยู่บนใบเสนอราคาและคำขอขาย**ทุกใบ** รวมถึงฉบับที่พิมพ์ส่งลูกค้า

- **เลขที่เอกสาร** 22px ตัวหนา สีส้มบนขาว
- **แถบ Grand Total** ตัวอักษรขาวบนพื้นส้ม — ตัวเลขที่สำคัญที่สุดบนใบ
- **เส้นคาดใต้หัวเอกสาร** (เส้นตกแต่ง ไม่ใช่ข้อความ จึงไม่อยู่ในเกณฑ์เดียวกัน)

เอกสารขาเข้าปิดช่องนี้ไปแล้วที่ P2b (teal `#0f766e` = 5.47:1)
**แต่ฝั่งขายยังใช้ส้มอยู่** และเป็นฝั่งที่เอกสารออกไปถึงคนนอกบริษัท

ทางแก้ที่เสนอ: เพิ่ม family `outbound` แล้วเลือกเฉดส้มที่เข้มพอ
เช่น `#c2410c` (วัดได้ 5.18:1) ซึ่งยังอ่านเป็นส้มของแบรนด์อยู่ แต่ผ่านเกณฑ์
**ห้ามแก้ `--c-primary` เอง** เพราะนั่นคือสีแบรนด์บนแถบซ้ายและโลโก้
ที่ตั้งใจให้คงที่ทุกหน้าจอ — ต้องแก้ที่ token ของเอกสารเท่านั้น

มีเทสต์บันทึกสภาพนี้ไว้ที่ `tests/doc-tokens.test.ts`
(`expect(contrast("#f97316", WHITE)).toBeLessThan(3)`)
ถ้าวันหนึ่งเทสต์ตัวนั้นแดง แปลว่าฝั่งขายถูกแก้แล้วและข้อนี้ปิดได้

### N-8. ตระกูล `isOverX` — เขียนครบ ไม่มีใครเรียก และข้อมูลจริงจะทำให้มันแดง

พบตอนไล่ตรวจหลัง P1c ว่ายังเหลือ "กฎที่ไม่กัดใคร" ที่ไหนอีก
ไล่ทุก export ในโมดูล domain แล้วดูว่าตัวไหนไม่มีคนเรียกเลยแม้แต่ในเทสต์

`isOverBilled()` ที่ต่อสายไปแล้วที่ N8 ไม่ได้เป็นตัวเดียว มีทั้งตระกูล

| ฟังก์ชัน | ไฟล์ | คนเรียก |
|---|---|---|
| `isOverReturn` | `lib/domain/sales-return.ts` | ไม่มี |
| `isOverReceive` | `lib/domain/sales-return.ts` | ไม่มี |
| `withinReturnPeriod` | `lib/domain/sales-return.ts` | ไม่มี |
| `qcBalanced` | `lib/domain/sales-return.ts` | ไม่มี |
| `isOverShipped` | `lib/domain/shipment.ts` | ไม่มี |
| `bpBlockingIssues` | `lib/domain/partner.ts` | ไม่มี |

**และต่างจากข้อ 11 ตรงที่ข้อมูลเดินไปถึงกฎได้จริง** วัดจาก seed ปัจจุบัน

```
ใบรับคืน 15 ใบ · บรรทัดรวม 22
  isOverReturn เป็นจริง 2 บรรทัด      ← คืนเกินจำนวนที่เคยส่งไป
  withinReturnPeriod เป็นเท็จ 2 ใบ    ← เลยกำหนดรับคืนแล้ว
```

แปลว่าถ้าวันนี้มีใครเรียก `isOverReturn()` ที่จุดเขียน จะมีสองบรรทัดถูกปฏิเสธทันที
กฎไม่ได้ผิด ข้อมูลไม่ได้ผิด — แค่ไม่มีใครถาม

`isOverShipped` ยิงไม่ออกกับข้อมูลชุดนี้ (0 จาก 25 บรรทัด) แต่ยังควรต่อสาย
เพราะเป็นกฎเดียวกันคนละเอกสาร

**ไม่แก้ในรอบนี้** — อยู่นอกขอบเขตงานใบขอซื้อทั้งหมด และการเปิดกฎที่ปฏิเสธ
เอกสารที่มีอยู่แล้วสองใบ ต้องให้คนตัดสินก่อนว่าจะทำอย่างไรกับสองใบนั้น
เป็นการตัดสินใจทางธุรกิจ ไม่ใช่การแก้โค้ด

ตอนต่อสาย ให้ทำแบบเดียวกับ `isOverBilled` ที่ N8 คือใส่ guard ที่ **จุดเขียน**
ไม่ใช่แค่ตั้งค่าเริ่มต้นในฟอร์ม และต้องมีเทสต์ที่พิสูจน์ว่ามีเคสถูกปฏิเสธจริง

### N-7. ใบลดหนี้ตั้งภาษี 7% ให้บรรทัดที่ยกเว้นภาษี — `|| 7`

พบตอนทำ P1b-2 ขณะไปหาตัวอย่างของรูปแบบ "ค่า default ที่ผิดแบบเงียบ ๆ"
ปรากฏว่าตัวอย่างนั้นยังมีชีวิตอยู่ในโค้ด

```
lib/domain/credit-note.ts:360     taxRate: num(it.taxRate) || 7,
```

`num()` คืน 0 สำหรับบรรทัดที่ยกเว้นภาษี และ `0 || 7` ได้ 7
โมดูลใบแจ้งหนี้เขียนเตือนเรื่องนี้ไว้เองที่ [lib/domain/invoice.ts:388-389](../lib/domain/invoice.ts)
ว่าให้ใช้ `?? 7` ไม่ใช่ `|| 7` และมี `billedTaxRate()` ที่ทำถูกอยู่แล้ว
แต่ใบลดหนี้ไม่ได้เรียกตัวนั้น

ผลกับผู้ใช้: ออกใบลดหนี้อ้างอิงใบแจ้งหนี้ Non VAT หรืออ้างอิงบรรทัดสินค้า
ที่ยกเว้นภาษี แล้วใบลดหนี้จะตั้ง VAT 7% กลับมาเอง เป็นเอกสารภาษี
ที่ออกให้ลูกค้าและใช้ยื่นสรรพากร

ทางแก้: เรียก `billedTaxRate()` ที่มีอยู่แล้ว หรืออย่างน้อยเปลี่ยนเป็น `?? 7`
พร้อมเทสต์ที่บรรทัดภาษี 0 ต้องออกมาเป็น 0

**ไม่แก้ในรอบนี้** เพราะเป็นเอกสารภาษีที่ยื่นสรรพากร ต้องมีคนตรวจด้านบัญชีก่อน
ไม่ใช่แก้เพราะโค้ดดูผิด — เหตุผลนี้หนักกว่าเรื่องขอบเขต

**และนี่คือรูปแบบ ไม่ใช่บั๊กเดี่ยว**

> ทางแก้ที่ถูกมีอยู่ในระบบแล้ว แต่ไม่ได้ถูกเรียกทุกที่ที่ควรเรียก

เหมือน `checkQuotedPrice()` ที่เขียนครบแล้วไม่มีใครเรียกเลย ต่างกันแค่รอบนี้
มีคนเรียกอยู่บ้าง — `billedTaxRate()` ถูกใช้ใน `billableLinesFrom()` แต่ไม่ถูกใช้
ใน `creditableLinesFrom()` ซึ่งอยู่คนละไฟล์

แบบนี้อันตรายกว่า "ไม่มีใครเรียกเลย" เพราะการ grep หาชื่อฟังก์ชันจะเจอผู้เรียก
แล้วดูเหมือนมันถูกใช้งานอยู่ **การนับว่ามีคนเรียกไม่พอ ต้องดูว่าทุกที่ที่ควรเรียก
ได้เรียกจริงไหม** — เวลาเขียน helper ที่ห่อกฎธุรกิจไว้ ให้ไล่หาทุกจุดที่คำนวณ
เรื่องเดียวกัน ไม่ใช่แค่จุดที่กำลังแก้อยู่

หมายเหตุ: `schemas/forms/credit-note.tsx:606` และ `schemas/forms/sales-invoice.tsx:782`
ใช้ `num(s.vatRate) || 7` เหมือนกัน แต่เป็นค่า default ระดับหัวเอกสาร
ตอนผู้ใช้ยังไม่กรอก — คนละเคส ต้องดูทีละจุด อย่าไล่แก้รวด

### A1b-1. ช่อง Rounding ในฟอร์มใบแจ้งหนี้เป็นตัวควบคุมที่ไม่มีผล — เรื่องเร่งของกลุ่มเอกสารภาษี

> **ข้อนี้มาก่อนข้ออื่นในกลุ่มเอกสารภาษี** (เจ้าของโปรเจกต์ตัดสิน 2026-08-09)
> เพราะอยู่บนใบกำกับซึ่งยื่นสรรพากร และเป็นตัวควบคุมที่ไม่มีผลแบบเดียวกับ N-1
> ต่างกันตรงที่ผลของ N-1 คือการแจ้งเตือน ส่วนของข้อนี้คือ**ตัวเลขเงิน**


พบตอน A1b ขณะตรวจว่า `rounding` กับ `withholdingTax` ยังคำนวณถูกไหม
เมื่อมีสามฟิลด์ใหม่เข้ามา — สูตรถูก แต่ค่าที่ป้อนเข้าสูตรไม่มีวันมาถึง

```
schemas/forms/sales-invoice.tsx   { type: "select", path: "rounding",
                                    options: opts(INV_ROUNDING) }
                                  → "None" · "Round to 0.01" · "Round to 1.00"
data/sales-invoices.ts            rounding: number
lib/domain/invoice.ts             const rounding = num(inv.rounding)
```

ผู้ใช้เลือก "Round to 1.00" ฟอร์มเขียนสตริงลงช่องที่ประกาศเป็นตัวเลข
แล้ว `num("Round to 1.00")` คืน **0** ทุกครั้ง — ปัดเศษไม่เคยเกิดขึ้นเลย
สักครั้งเดียวนับตั้งแต่มีช่องนี้ และหน้าจอไม่มีอะไรบอกว่าไม่เกิด

เป็นรูปแบบเดียวกับสวิตช์ In-App ในข้อ N-1 เป๊ะ ๆ — **ตัวควบคุมที่บันทึกว่า
ตัวเองมีผล** ต่างกันแค่อันนี้อยู่บนเอกสารภาษี

ต้องตัดสินก่อนแก้: จะให้เลือก *วิธี* ปัด (แล้วโดเมนคำนวณส่วนต่างเอง)
หรือให้พิมพ์ *จำนวน* ที่ปัด (แล้วเปลี่ยน select เป็น number)
สูตรตอนนี้บวก `rounding` ตรง ๆ ซึ่งเข้ากับแบบหลัง แต่ป้ายบอกว่าแบบแรก
**ไม่แก้ในรอบนี้ด้วยเหตุผลเดียวกับ N-7** — ตัวเลขบนใบกำกับที่ยื่นสรรพากร

เทสต์ที่ต้องมีตอนแก้: เลือกวิธีปัดแล้วยอดรวมต้องขยับจริง

### A1b-2. `headerDisc` ของใบลดหนี้ยังเป็นเปอร์เซ็นต์ ทั้งที่สายที่เหลือเป็นบาทแล้ว

A1b รวมนิยามให้ใบเสนอราคา · คำขอขาย · ใบสั่งขาย · ใบแจ้งหนี้ ใช้ **จำนวนเงิน**
เหมือนกันหมด ใบลดหนี้ไม่ได้อยู่ในสายนั้นจึงไม่ได้แก้ไปด้วย

```
lib/domain/credit-note.ts:87   round2(afterLine * (num(cn.headerDisc) / 100))
schemas/forms/credit-note.tsx  label: "Header Discount (%)"
```

ตัวมันเองสอดคล้องกัน (ฟอร์มบอก % สูตรคิด %) และ `creditableLinesFrom()`
ไม่ได้คัดลอกค่าหัวบิลจากใบแจ้งหนี้ จึงยังไม่มีใครส่งบาทเข้าไปให้อ่านเป็นเปอร์เซ็นต์
**แต่ชื่อฟิลด์เดียวกันมีสองความหมายในระบบเดียว** ซึ่งเป็นกับดักที่ A1b
เพิ่งเก็บไปหนึ่งจุด และวันที่ใครต่อสายใบลดหนี้กับใบแจ้งหนี้คือวันที่มันกัด
ส่วนลด 137 บาทจะกลายเป็น 137%

เป็นเอกสารภาษีเช่นกัน ต้องให้บัญชีตรวจก่อน — เหตุผลเดียวกับ N-7

### N-2. `Partially Delivered` ที่ `delivered` ยังเป็น 0 — ต้องแก้ก่อนทำ Reports

ตั้งแต่ N6 สถานะนี้ถูกตั้งจากสองที่ที่มีความหมายไม่เหมือนกัน:

| ที่ | เมื่อไหร่ | `delivered` ตอนนั้น |
|---|---|---|
| `pickComplete` | หยิบของไม่ครบ | **0 ทุกบรรทัด** |
| `packCreateDelivery` (เพิ่มที่ N8) | คลังยืนยันส่งได้ไม่ครบ | **0 ทุกบรรทัด** |
| `doConfirmDelivery` | ส่งจริงแล้วบางส่วน | > 0 |

**N8 ทำให้ข้อนี้เร่งขึ้น ไม่ใช่เบาลง** — ตอนนี้มีสามที่ที่เขียนสถานะนี้ และสองในสาม
เขียนตอนที่ยังไม่มีของออกจากคลังเลย ยิ่งต้องแยกให้จบก่อนเริ่มทำ Reports

ตัวเลขจำนวน (`deliveredQty`, `outstandingQty`) คำนวณจากบรรทัด จึงยังถูกอยู่
**ที่โกหกคือสถานะ** — Reports ที่จัดกลุ่มตามสถานะจะนับใบที่ยังไม่ได้ส่งอะไรเลย
เข้าไปอยู่ในถัง "ส่งแล้วบางส่วน" ทั้ง "ส่งแล้ว" และ "ค้างส่ง" จึงเพี้ยนพร้อมกัน
และวันที่เปลี่ยนสถานะก็จะชี้ไปที่วันหยิบของ ไม่ใช่วันส่ง ทำให้การวิเคราะห์ตรงเวลาผิดตาม

มีเทสต์ยืนยันสภาพนี้อยู่แล้วที่ `tests/short-stock.test.tsx:287-291`
ซึ่ง assert ว่า `so.items[0].delivered === 0` ขณะที่สถานะเป็น `Partially Delivered`

**ทางแก้ที่เสนอ — แยกฟิลด์ ไม่ใช่เพิ่มสถานะ**

เสนอให้เพิ่ม flag เช่น `so.shortPicked` (หรือ `fulfilmentAtRisk`) แล้ว
ให้ `pickComplete` เซ็ต flag นี้แทนการเปลี่ยนสถานะ
คงสถานะไว้ที่ `Picking` จนกว่าจะมีของออกจริง แล้วให้ badge กับใบงานอ่าน flag

เหตุผลที่เลือกแยกฟิลด์แทนการเพิ่มสถานะใหม่ (เช่น `Short Picked`):

- สถานะของ S/O ถูก hardcode เป็น array ไว้ **16 จุดใน 9 ไฟล์** (นับแล้ว ไม่ได้ประมาณ):
  `schemas/sales-order.tsx` 7 จุด · `lib/domain/outbound.ts` 2 จุด ·
  `data/sales-orders.ts` (ตัว enum) · `lib/badges.ts` (`SO_TONE`) ·
  `lib/workflows-outbound.tsx` · `lib/domain/invoice.ts` · `lib/domain/dashboard.ts` ·
  `lib/domain/sales-return.ts` · `schemas/forms/sales-order.tsx` อย่างละจุด
  เพิ่มสถานะแปลว่าต้องไล่ครบทุกจุด **พลาดจุดเดียว ใบสั่งขายจะหายไปจากลิสต์เงียบ ๆ
  หรือปุ่มที่ควรกดได้จะหายไป** — เป็นบั๊กชนิดเดียวกับที่ N7 เพิ่งเจอ คือชิ้นส่วนถูกหมด ผิดที่รอยต่อ
- แยกฟิลด์เป็นการเพิ่ม ไม่ใช่การขยาย enum โค้ดที่อ่านสถานะอยู่แล้วไม่ต้องแก้เลย
  และ Reports ที่จัดกลุ่มตามสถานะจะถูกทันทีโดยไม่ต้องรู้เรื่อง flag นี้
- คุณค่าจริงของ N6 คือ**รู้เร็ว** ซึ่งมาจากการแจ้งเตือน ไม่ได้มาจากตัวสถานะ
  flag เก็บคุณค่านั้นไว้ครบ โดยไม่ต้องยืมความหมายของช่องที่ Reports จะเอาไปจัดกลุ่ม

ราคาที่ต้องจ่าย: badge กับหน้ารายการต้องอ่านสองอย่าง (สถานะ + flag)
ถ้าไม่ทำ UI ให้ flag ขึ้น สัญญาณเตือนล่วงหน้าจะมองไม่เห็น — ต้องทำพร้อมกัน

**เทสต์ที่ต้องมีตอนแก้:** ใบที่หยิบไม่ครบต้อง**ไม่**ถูกนับใน Reports ช่อง "ส่งแล้วบางส่วน"

### 13. ใบเสนอราคาถูกปิดเป็น Converted ตั้งแต่ตอนเซฟร่างคำขอขาย

`lib/domain/sales-request-draft.ts:567-569` เซ็ต `qt.srRef` และ `qt.status = "Converted"`
ตั้งแต่การเขียนครั้งแรก — ก่อนที่คำขอจะถูก submit ด้วยซ้ำ ไม่ต้องพูดถึงอนุมัติ

ถ้าคำขอนั้นถูกยกเลิกหรือลบทีหลัง ใบเสนอราคาจะค้างเติ่ง:
สถานะ Converted, `srRef` ชี้ไปยังเอกสารที่ไม่มีอยู่แล้ว และแปลงใหม่ไม่ได้อีกเลย

ควรปิดตอน `srSubmit` หรือตอน convert ไม่ใช่ตอนเซฟร่าง
ของเดิมอยู่ก่อนงานชุดนี้ ไม่ได้เกิดจาก N1–N7 แต่ผู้ใช้จะเจอแน่

### N-1. หน้า Notification Settings ยังไม่ได้ต่อกับตัวส่งจริง

**บรรเทาแล้ว** — ถอดสวิตช์ออก ทำหน้าให้อ่านอย่างเดียว และเขียนบอกไว้บนหน้า

ของเดิมสวิตช์ In-App เขียน `NOTIFICATIONS[].inApp` แล้ว `lib/domain/notify.ts`
ซึ่งเป็นตัวส่งเพียงตัวเดียว ไม่เคยอ่าน flag นั้น ปิดแล้วการแจ้งเตือนยังมาเหมือนเดิม
แล้ว Audit Log ยังบันทึกว่าแอดมินปิดสำเร็จ — **ตัวควบคุมที่บันทึกหลักฐานเท็จว่าตัวเองมีผล**

ที่ยังต้องทำก่อนเปิดใช้จริง:

1. ให้ `notify()` อ่านค่าจากตารางนี้ก่อนส่ง
2. คอลัมน์ `roles` เป็นรายชื่อที่พิมพ์ไว้กับมือ ขัดกับกฎข้อ 1 ของ `notify.ts`
   ที่คำนวณผู้รับจากตารางสิทธิ์ตอนส่ง — สองอันนี้ขัดกันอยู่แล้ว และตารางนี้คือฝั่งที่ผิด
3. มีตัวส่งจริงเฉพาะกลุ่ม Approval เท่านั้น อีก 8 แถว
   (Low Stock, Near Expiry, QC Failed, Credit Limit, Overdue Invoice,
   Late Shipment, Failed Login, Approval Overdue) ไม่มีโค้ดยิงเลยสักที่
4. ช่อง Email ต้องมีระบบส่งเมลก่อน ซึ่งยังไม่มี

**เทสต์ที่ต้องมีตอนเปิดใช้:** ปิดการแจ้งเตือนหนึ่งรายการแล้ว `notify()` ต้องไม่สร้าง item

ตอนนี้มีเทสต์กันไว้ที่ `tests/administration.test.tsx` ว่าหน้านี้ต้องไม่มีตัวควบคุมที่อ้างว่าเปลี่ยนค่าได้

### N-4. ข้อมูลตัวอย่างมีเอกสารสองจักรวาลที่ไม่เคยเจอกัน

พบตอนทำ N9 ขณะไล่สายจากเลข tracking

| กลุ่ม | รหัสที่ใช้ |
|---|---|
| ใบเสนอราคา → คำขอขาย → ใบสั่งขาย → หยิบ → แพ็ค → ใบส่งของ | `QT2506-0001` · `SR2506-0001` · `SO2506-0001` · `DO2507-0001` |
| ใบกำกับ + รอบขนส่ง | `SO-2026-000031` · `DO-2026-000014` · `INV-2026-000025` |

`data/shipments.ts` และ `data/sales-invoices.ts` ถูกเขียนแยกจากสายการขาย
และอ้างถึงเอกสารที่ **ไม่มีอยู่จริงใน `data/`** สักฉบับ
ผลคือไล่ย้อนกลับจากเลข tracking แล้วตันที่รอบขนส่งทุกแถว — ดูเหมือนฟังก์ชันพัง ทั้งที่ข้อมูลพัง

**บรรเทาแล้วบางส่วน:** เดินสายหนึ่งเส้นให้ครบจริง
`AF260801001 → SHP-2026-000031 → DO2507-0001 → SO2506-0001 → SR2506-0001 → QT2506-0001`
มีเทสต์เดินสายนี้บนข้อมูลที่ shipped จริง ไม่ได้ประกอบขึ้นในเทสต์
และมีเทสต์อีกตัวยืนยันว่าแถวที่เหลือยัง**ตัน** เพื่อให้ช่องว่างนี้มองเห็นได้ ไม่ใช่ถูกกลบ

ที่ยังต้องทำ: ต่อแถวที่เหลือ หรือสร้างชุดข้อมูลตัวอย่างใหม่ที่มี code space เดียว
**เกี่ยวข้องกับข้อ 11 โดยตรง** — เป็นอาการเดียวกันคนละที่ คือรหัสคนละชุดทำให้กฎที่เขียนไว้เดินไปไม่ถึงข้อมูล

### N-5. `ProductRow.projected` / `availTotal` เขียนแล้วไม่มีใครอ่าน

พบตอนทำ P0b `decorateProducts()` คำนวณ `p.availTotal` กับ `p.projected` จากยอดรวมรายคลัง
แต่**ไม่มีที่ไหนในแอปอ่านสองตัวนี้เลย** ทุกหน้าจอไปทาง `productStock()` ซึ่งอ่านฟิลด์แบน ๆ ของสินค้า

**เรื่องนี้คือสาเหตุที่บั๊ก `projected` อยู่ได้นาน — คอมเมนต์ที่กำหนดนิยามยืนอยู่เหนือฟิลด์ที่ตายแล้ว
ส่วนสูตรที่ผู้ใช้เห็นจริงอยู่คนละที่และไม่ตรงกับคอมเมนต์นั้น**

และเพราะฟิลด์นั้นไม่มีคนอ่าน จึงไม่มีใครสังเกตว่า**ตัวนิยามเองก็ผิด** — หัก Back Order
อย่างเดียว ไม่หัก Reserved ทำให้ P0b ที่ "แก้ให้ตรงตามนิยาม" ยิ่งทำให้ตัวเลขผิดหนักขึ้น
ต้องตามแก้ที่ P0c อีกรอบ นิยามที่ไม่มีโค้ดที่ทำงานจริงคอยตรวจสอบ จะเน่าเงียบ ๆ แบบนี้

ยังมีความต่างอีกชั้นที่ยังไม่ได้ปิด: `decorateProducts()` ใช้ผลรวมจาก `whRows`
ส่วน `productStock()` ใช้ `p.onHand` / `p.reserved` / `p.onOrder` ตรง ๆ
สินค้าที่มีทั้งสองอย่างและไม่ตรงกัน จะให้คำตอบต่างกัน

ทางเลือก: ลบสองฟิลด์ทิ้ง (ตามแบบข้อ 15 ที่ลบ `billType` เพราะไม่มีคนอ่าน)
หรือให้ `productStock()` อ่านจากยอดรวมรายคลังแล้วเหลือแหล่งเดียว
**ยังไม่ทำในคอมมิตเดียวกับที่แก้สูตร** เพราะจะกลบ diff ของการแก้สูตร

### N-3. กฎ "คู่ค้าสถานะ Draft เปิด S/O ไม่ได้" ไม่มีข้อมูลตัวอย่างให้กัด

กฎตามสเปกข้อ 3.3 เขียนครบและมีเทสต์ 18 ตัวใน `tests/draft-partner.test.ts`
แต่ `data/partners.ts` มีคู่ค้าสถานะ `Draft` อยู่ **0 ราย** (Active 9 · Inactive 1)

แปลว่าในแอปที่รันจริง กฎนี้ไม่เคยปฏิเสธใคร มันกัดเฉพาะในเทสต์ที่ประกอบคู่ค้า Draft ขึ้นมาเอง
เป็นรูปแบบเดียวกับ `maySignAt` ก่อน N1 เป๊ะ ๆ คือ **เขียวในเทสต์ แต่มองไม่เห็นในเดโม**

ทางแก้: ใส่คู่ค้าสถานะ `Draft` อย่างน้อยหนึ่งรายลงข้อมูลตัวอย่าง
ให้เซลล์กด "เปิดใบสั่งขาย" แล้วโดนปฏิเสธได้จริงบนหน้าจอ

**บทเรียนที่กว้างกว่าตัวข้อนี้:** กฎตายได้สองแบบ — ไม่มีใครเรียก
หรือ **ไม่มีข้อมูลที่เดินไปถึงมัน** แบบหลังตรวจไม่เจอด้วยการอ่านโค้ด
ต้อง grep ข้อมูลตัวอย่างหาสถานะที่กฎนั้นรอ ก่อนจะเชื่อว่าเทสต์เขียวแปลว่ากฎมีชีวิต

### 1. `qtCancel` และ `qtDelete` ไม่มีด่านตรวจสิทธิ์

workflow ของใบเสนอราคาทุกตัวเรียก `denied(ctx, "quotation", …)` ก่อน
สองตัวนี้แก้ข้อมูลโดยไม่ตรวจอะไรเลย (`lib/workflows-outbound.tsx:659`, `:673`)
เป็นรูที่มีมาก่อน ไม่ได้เกิดจากงานชุดนี้

### BP-2. เอา Price Group ออกจากฟอร์มแล้ว แต่ `price-tier.ts` ยังอ่านอยู่

`cls.priceGroup` ถูกถอดออกจากฟอร์มคู่ค้าตามที่สั่ง แต่ฟิลด์ยังอยู่ในระเบียน
เพราะ `lib/domain/price-tier.ts` ใช้เทียบว่าชั้นราคาที่เลือกตรงกับกลุ่มราคา
ของคู่ค้าไหม **ตอนนี้จึงเป็นกฎที่มีคนอ่าน แต่ไม่มีใครเขียนได้อีกแล้ว** —
ค่าที่มีอยู่คือค่าที่ค้างจาก seed 8 แถว ของใหม่จะว่างตลอด

**และตอนนี้มีรายการราคาสองชุดในระบบ** ช่อง Price List บนฟอร์มคู่ค้าใส่ชื่อ
ตัวอย่างไว้ก่อนตามที่สั่ง — ราคาเอกชน · ราคาราชการ · ราคาสอบราคา — ขณะที่
`data/price-lists.ts` มี PL-STD-2026 และพวก และ `pricing.ts` มีสี่ชั้นราคา
(government ≥ private ≥ dealer ≥ last) สามชุดนี้ต้องยุบเหลือชุดเดียวตอนทำ
เรื่องราคา ไม่ใช่ตอนนี้ แต่ต้องยุบ

ต้องเลือก ไม่ใช่ปล่อย
  ก) ให้กลุ่มราคามาจาก Customer Group แทน (คลินิก/โรงพยาบาลรัฐ/ดีลเลอร์
     บอกชั้นราคาได้อยู่แล้ว) แล้วลบ `cls.priceGroup` ทิ้ง
  ข) เอาช่องกลับเข้าฟอร์ม
  ค) ลบการเทียบใน `price-tier.ts` ทิ้งไปด้วย

### UI-1. เครื่อง Quick View ยังอยู่ในโค้ด แต่ไม่มีใครเรียกแล้ว

กดแถวแล้วเข้าหน้าระเบียนเลย ตามที่สั่ง — ผู้เรียก `ctx.quickView` หมดไปทั้ง 27 จุด
และถอด `<QuickViewHost />` ออกจาก app shell แล้ว จึงไม่มีทางโผล่ในแอปอีก

ที่ยังเหลือคือตัวเครื่อง — `QuickViewHost.tsx` · ช่อง `quickView` ใน store ·
`openQuickView` / `closeQuickView` · `ActionCtx.quickView` ทั้งหมดเป็นโค้ดที่
ไม่มีใครเรียก ซึ่งเป็นสิ่งที่เอกสารชุดนี้เตือนไว้ทั้งหน้าว่าทำให้คนอ่านเชื่อว่า
มีกลไกที่ไม่มีอยู่จริง

ไม่ลบในรอบนี้เพราะ **เทสต์เก้าไฟล์ mount `<QuickViewHost />` ไว้ใน harness**
การลบต้องไปแก้ทั้งเก้าไฟล์ ซึ่งกลบ diff ของงานที่กำลังตรวจ
ตอนลบ ให้ลบทั้งสี่ชิ้นพร้อมกัน อย่าเหลือช่องใน store ไว้เฉย ๆ

### UI-2. ช่องในกริดไม่มีชื่อที่เครื่องอ่านหน้าจอจับได้

พบตอนเขียนเทสต์ที่เรนเดอร์ฟอร์มจริงเพื่อตรวจ dropdown อำเภอ —
`getByLabelText("จังหวัด")` หาไม่เจอ เพราะกริดแบบ stacked เขียนป้ายเป็น
`<span>` ลอย ๆ เหนือช่อง ไม่ใช่ `<label htmlFor>` และช่องเองก็ไม่มี `aria-label`

แปลว่าทุกช่องในทุกกริดของทุกฟอร์มไม่มีชื่อที่ assistive technology จับได้
ผู้ใช้ที่ใช้เครื่องอ่านหน้าจอได้ยินแค่ "combo box" เปล่า ๆ
ฟอร์มเดี่ยว (ไม่ใช่กริด) ไม่มีปัญหานี้ — มันผูก label ถูกอยู่แล้ว

ทางแก้: ใส่ `aria-label={c.label}` ให้ `GridCell` หรือผูก `htmlFor` กับ id
ต่อแถวต่อคอลัมน์ ทางแรกเล็กกว่ามากและพอสำหรับกรณีนี้
กระทบทุกกริด จึงควรทำเป็นขั้นของตัวเองพร้อมกวาดเทสต์ที่ต้องเปลี่ยนวิธีหา element

### C-1. `showDiscount` ถูกรายงานให้แอดมินดู แต่ไม่ได้ตัดสินอะไร

พบตอนกลุ่ม C ขณะไล่หาคีย์ตายใน `PrintConfig` — เจอสามตัว ปิดไปสองแบบต่างกัน

```
showPackage   ไม่มีใครอ่านเลยแม้แต่ที่เดียว              → ลบ
showDueDate   ไม่มีใครอ่านในโค้ดแอป มีแต่เทสต์ที่ assert  → ทำให้มีความหมาย
showDiscount  มีผู้อ่านที่เดียว คือหน้า Admin › Templates → ยังไม่แตะ
```

ตัวที่สามต่างจากสองตัวแรก มันมีผู้อ่าน แต่ผู้อ่านคือหน้าที่**เอาค่าไปแสดง**
ว่า "Discount: Yes/No" ส่วนคอลัมน์ส่วนลดบนแผ่นจริงตัดสินโดย `itemColumns`
แอดมินจึงอ่านคำตอบจากช่องที่ไม่ได้เป็นคนตอบ ถ้าวันหนึ่งสองอย่างไม่ตรงกัน
หน้าจอจะบอกอย่าง กระดาษจะเป็นอีกอย่าง และไม่มีอะไรทักท้วง

ตรวจแล้ววันนี้ยังตรงกันทุกเอกสาร จึงยัง**ไม่โกหก** แค่รอวันที่จะโกหก

ทางเลือกสองทาง ต้องเลือก ไม่ใช่ปล่อย
  ลบทิ้ง แล้วให้หน้า Templates อ่าน `itemColumns.includes("discount")` ตรง ๆ
  หรือให้ `showDiscount` เป็นคนตัดสินจริง แล้วให้ `itemColumns` ตามมัน
ทางแรกเล็กกว่าและตรงกับที่เกิดขึ้นจริงอยู่แล้ว

### A1c-1. ฟอร์ม Business Partner ยังสร้างระเบียนหลัง double cast

A1c ถอด `as unknown as` ออกจาก 24 จุดที่สร้างระเบียนใหม่ เหลือจุดเดียว
มีทริปไวร์คุมไว้ที่ `tests/record-writes.test.ts` พร้อมเหตุผลและจำนวนที่อนุญาต

ติดตรงที่ฟอร์มนี้ประกอบระเบียนจาก grid ที่ชนิดหลวม แล้ว spread เข้าไปตรง ๆ
หกชุด — `contacts` · `banks` · `docs` · `addresses` · `images` · `supplierItems`
`{ ...row }` จาก `GridRow` ไม่บอกอะไรคอมไพเลอร์เลย จึงตรวจไม่ได้ว่าสิ่งที่ลงระเบียน
เป็นผู้ติดต่อจริงไหม

**ตอนรันไม่มีปัญหา** ค่าที่ spread มาครบเพราะมันมาจากระเบียนตั้งแต่แรก
ที่ขาดคือหลักฐาน ไม่ใช่ข้อมูล

ทางแก้: เขียน mapping ทั้งหกออกมาทีละฟิลด์ (ทำไปแล้วสองชุดใน A1c แล้วถอยกลับ
เพราะมันบานเกินขั้นที่กำลังตรวจ) **ความเสี่ยงจริงของงานนี้คือทำฟิลด์หล่น** —
ฟิลด์ที่มีอยู่ตอนรันแต่ไม่มีใครลิสต์ จะหายไปเงียบ ๆ ตอนเปลี่ยนเป็น mapping ชัด ๆ
ต้องไล่เทียบกับระเบียนจริงทีละช่อง ไม่ใช่เขียนจากที่เห็นในฟอร์ม

### 9. Line ending ปนกันทั้ง repo

`data/sales-orders.ts`, `lib/domain/quotation-draft.ts`, `lib/domain/sales-request-draft.ts`
เป็น CRLF ส่วน `data/quotations.ts`, `lib/workflows-outbound.tsx`, `lib/domain/invoice.ts` เป็น LF

ทางแก้ที่เสนอ: เพิ่ม `.gitattributes` + `.editorconfig` แล้ว normalise ทั้ง repo
ใน commit เดียวที่ไม่มีอย่างอื่นปนมาเลย — normalise พร้อมงานจริงจะกลบ diff ของงานจริง

**กฎที่ข้อนี้สอน สำคัญกว่าตัวการแก้:** สคริปต์ search-and-replace ที่ยึด `$`
จะ match **ศูนย์บรรทัด** ในไฟล์ CRLF แล้วรายงานว่าสำเร็จ
ต้อง assert จำนวนที่แทนที่ได้เสมอ ถ้าแก้ไม่เกิน 5 จุดให้ใช้ Edit tool แทน — มันอ่านไฟล์จริงและพลาดแบบนี้ไม่ได้

### 7. ระบบไม่เคยผลิต PDF จริง

เครื่องพิมพ์ render HTML/CSS ที่สเกล mm แล้วเรียก `window.print()`
ผู้ใช้กด Save as PDF จาก dialog ของเบราว์เซอร์เอง ไม่มีอะไรถูกเขียนลง storage
จึงไม่มีอะไรเสิร์ฟซ้ำแบบ byte-for-byte ได้ทีหลัง

revision ของใบเสนอราคาเก็บเป็น snapshot ข้อมูล (`QtRevision`) แล้ว render
ผ่าน template ปัจจุบัน ซึ่งพอใช้ภายใน แต่ไม่ใช่สำเนาที่ตรงกับแผ่นที่ลูกค้าถืออยู่
ถ้าต้องการหลักฐานระดับกฎหมาย ต้องมี backend + file storage ก่อน ซึ่งยังไม่มีทั้งคู่

### 4. ตระกูลเอกสารขาออกไม่มี audit trail

quotation, sales request, sales order, picking, packing, delivery order
บันทึกแค่ `log()` → `history[]` ขณะที่ credit note กับ invoice เก็บ `audit[]`
แบบมีโครงสร้าง (from→to รายฟิลด์)

ถ้าจะเพิ่ม audit ให้เพิ่มทั้งตระกูลพร้อมกัน ไม่ใช่ทีละใบ

### 2. `SR_STATUS` ถูก export จากสองไฟล์ด้วยความหมายคนละอย่าง

`data/sales-requests.ts:94` = สถานะเอกสาร
`data/sales-reps.ts:66` = สถานะการจ้างงาน

วันนี้ยังไม่มีไฟล์ไหน import ทั้งคู่ จึงยังไม่พัง แต่เป็นกับดักตอนแก้ไฟล์ใดไฟล์หนึ่ง

### 10. ฟอร์มพิมพ์ใบเสนอราคาไม่มีสำเนา ACCOUNTING ต่างจาก `BASE`

`BASE.supportedCopyTypes` มี ORIGINAL / CUSTOMER / COMPANY / ACCOUNTING / REPRINT
แต่ config ของ `quotation` override เหลือสี่ ตัด ACCOUNTING ออก

บัญชีลงรายได้จากการขายที่มาจากใบเสนอราคาเหมือนรายการอื่น การตัดออกจึงอาจตั้งใจ
หรืออาจหลงเหลือจากตอนที่ฟอร์มยังไม่นิ่ง — **ต้องถามคนออกแบบฟอร์ม อย่าเดา
และอย่า "แก้" ด้วยการเพิ่มสำเนาเข้าไป** เพราะนั่นคือการเปลี่ยนฟอร์มที่ใช้งานอยู่ทุกวัน

### 8. พิมพ์หลายใบพร้อมกันจากหน้ารายการยังทำไม่ได้

ถูกยกขึ้นมาสองครั้ง (ขั้นที่ 4 และ 8) และเลื่อนทั้งสองครั้ง
ตั้งใจกันไว้นอกขอบเขตตอนที่กำลังขยายเครื่องพิมพ์อยู่

### 5. `TextField` ถูก copy-paste ข้ามไฟล์ workflow

อยู่ใน `workflows-adjustment.tsx`, `workflows-credit-note.tsx`, `workflows-outbound.tsx` ฯลฯ
การที่แต่ละไฟล์ถือสำเนาของตัวเองคือ convention เดิมของ repo
การรวบเป็นที่เดียวเป็นการตัดสินใจแยกต่างหาก

### PM-1. โปรแคตตาล็อก 234 แถวไม่เคยถูกใช้คำนวณ

`data/pricelist_master.json` มี 234 แถวจาก 807 ที่กรอก `promo_min_qty` และ
`promo_free_qty` ครบทั้งคู่ (มี `promo_catalog` เป็นข้อความ 242 แถว · `promo_legacy` 199 แถว)
เป็นเงื่อนไขซื้อ-แถมของผู้ผลิตที่ generate ติดมากับไฟล์ราคา

grep ทั้ง repo แล้ว **สองฟิลด์นี้ถูกอ้างถึงอย่างละสองที่เท่านั้น** — ที่ประกาศ type
ใน `lib/domain/pricing-master.ts` และที่ช่องแสดงผลในแท็บ Promotion ของ
`schemas/price-list-master.tsx` ไม่มีในเทสต์สักตัว ไม่มีฟังก์ชันไหนรับเข้าไป
ไม่มีการคูณ ไม่มีการเทียบ มันถูกส่งเข้า `fmt()` แล้วขึ้นจอ จบตรงนั้น

แม้แต่ `hasPromo` ก็ไม่ได้อ่านสองช่องนี้ — `lib/domain/price-master.ts` derive จาก
`promo_catalog || promo_legacy` ซึ่งเป็น**ข้อความ** ไม่ใช่ตัวเลข แล้วป้อนไปแค่แท็บ
"มีโปรโมชั่น" กับ filter chip

**รูปแบบเดียวกับ N-3 แต่กลับด้าน** — N-3 คือกฎมีชีวิตที่ไม่มีข้อมูลเดินไปถึง
ข้อนี้คือข้อมูล 234 แถวที่ไม่มีกฎไหนมารับ ทั้งคู่มองไม่เห็นจากการอ่านโค้ดฝั่งเดียว

**ถ้าจะเปิดใช้ ต้องตรวจก่อนว่าเงื่อนไขยังตรงกับที่ตกลงกับผู้ผลิต** — และต้องรู้ตัวว่า
การต่อสายคือการเปิดโปร 234 ตัวพร้อมกันในวันเดียว โดยไม่มีใครอนุมัติ ไม่มีงบ
ไม่มีวันหมดอายุ และไม่มีใครในบริษัทรู้ว่ามีอยู่ นั่นคือการเปลี่ยนวิธีขายทั้งบริษัท
ไม่ใช่ compatibility layer ของโมดูลโปรโมชั่น

> เจอตอนสำรวจก่อนทำโมดูล Promotion ตอนแรกสั่งให้ทำชั้น fallback
> (ไม่เข้าโปรใหม่ → ตกลงมาใช้ของเดิม) แล้วกลับคำหลังเห็นผล grep
> เพราะ "รักษาพฤติกรรมเดิม" กับ "เปิดใช้ของที่ไม่เคยทำงาน" ไม่ใช่เรื่องเดียวกัน

---

## ปิดแล้ว

**กลุ่ม A. ช่องที่ตัวแก้ไขเก็บค่าแล้วระเบียนไม่มีที่ใส่** — ปิดที่ A1 · A1b · A2a · A2b

รูปเดียวกันทั้งกลุ่ม: ตัวแก้ไขถาม ผู้ใช้กรอก แล้วค่าหายตอนกดเซฟ
ความต่างโผล่บนกระดาษที่มีคนลงมือทำตามไปแล้ว

| ช่อง | ปิดที่ | สิ่งที่ผู้ใช้เจอตอนยังไม่ปิด |
|---|---|---|
| headerDisc · freight · otherCharges | A1 · A1b | ลูกค้าได้ยอดที่ไม่ตรงกับที่ตกลง แล้วใบแจ้งหนี้เก็บไม่ครบ |
| ที่อยู่จัดส่ง 6 ช่อง | A2a | ของไปสำนักงานใหญ่ทั้งที่ตกลงกันว่าส่งสาขา |
| shipInstruction | A2a | "ส่งเช้าเท่านั้น" ไม่มีค่าสำรองให้หยิบมาแทน หายไปเฉย ๆ |
| deliveryDate | A2b | คำขอขายครบกำหนดตามวันที่ราคาหมดอายุ ไม่ใช่วันที่รับปาก |
| internalNote | A2b | เซลล์เสียบันทึกของตัวเองทุกครั้งที่เซฟ |
| internalRef | A2b | ลบ — ไม่มีใครเขียน ไม่มีใครอ่าน |

> **บทเรียนที่แพงที่สุดของกลุ่มนี้: ลำดับตอนอ่านกลับ**
> `draftFromQuotation` จบด้วย `applyCustomer()` ซึ่งเขียนทับที่อยู่ทั้งหกช่อง
> ด้วยค่าจากทะเบียนคู่ค้า ถ้าอ่านค่าจากระเบียนก่อนแล้วเรียกทีหลัง
> ก็เท่ากับไม่ได้เก็บอะไรเลย — และเทสต์ที่ตรวจแค่ "เซฟแล้วมีค่าในระเบียน"
> จะเขียวสนิท ต้องตรวจรอบที่สอง คือเปิดกลับมาแก้แล้วยังอยู่

**กลุ่ม C. คีย์ตายใน `PrintConfig`** — ปิดสองในสาม เหลือข้อ C-1 ข้างบน

`showPackage` ลบ (ไม่มีผู้อ่านเลย) · `showDueDate` ทำให้มีความหมาย

ตัวที่สองน่าสนใจกว่า เพราะมันตายคู่กับข้อความคงที่หนึ่งบรรทัด
`"กรุณาชำระเงินภายในวันที่ครบกำหนด"` — บอกลูกค้าให้จ่ายภายในวันครบกำหนด
โดยไม่บอกว่าวันไหน บนเอกสารที่เป็นฝ่ายรู้อยู่ฝ่ายเดียว
ตอนนี้ flag ตัดสินว่าจะทวงหรือไม่ และวันที่มาจากระเบียนจริง
**ครึ่งหนึ่งที่ไม่มีคนอ่าน กับอีกครึ่งที่ไม่มีวันที่ เป็นช่องว่างเดียวกัน**

> ระหว่างทางเจอว่าใบส่งของแบบมีราคาพิมพ์วันส่งของใต้ป้าย "ครบกำหนด"
> (`dueDate: so?.deliveryDate`) — ป้ายผิดบนเอกสารที่ส่งถึงลูกค้า
> เปลี่ยนเป็น `deliveryDate` ซึ่งพิมพ์วันเดียวกันใต้ป้ายที่ถูก

> และทริปไวร์ศักราชจับเทสต์ที่ผมเพิ่งเขียนเอง — มันบวก 543 เพื่อเทียบวันที่
> บนกระดาษ ซึ่งคือการที่เทสต์ถือกฎศักราชฉบับของตัวเอง ตรงกับที่ N-9c
> จับ `tests/dashboard.test.tsx` ได้เป๊ะ ๆ แก้เป็นเทียบกับแถว dueDate
> บนแผ่นเดียวกัน ไม่ต้องรู้เรื่องศักราชเลย และได้ของแถมคือพิสูจน์ว่า
> ข้อความทวงกับแถวในหัวเอกสารไม่มีทางไม่ตรงกัน

**BP-1. อำเภอกับตำบลเป็น dropdown แล้ว** — ปิดด้วยการ**ดึงข้อมูลจริง ไม่ใช่เขียนเอง**

เดิมมีแต่ 77 จังหวัด กับ 50 เขต กทม. (จาก sales area master) อำเภออีก 76 จังหวัด
กับตำบลทั้งประเทศไม่มีเลย ทางแก้ไม่ใช่พิมพ์รายชื่อออกมาเอง —
**อำเภอที่แต่งขึ้นอ่านเหมือนอำเภอจริงทุกประการ** และราคาของการผิดคือรถไปผิดที่

ดึงชุดข้อมูลมาเก็บที่ `data/thai-geography.json` (77 · 928 · 7,436 พร้อมรหัสไปรษณีย์
ทุกแถว) ระบุแหล่งที่มาและวันที่ดึงไว้ในไฟล์

**สิ่งที่ต้องตรวจก่อนใช้ไม่ใช่ว่าไฟล์อ่านได้ แต่คือมันตรงกับสิ่งที่แอปเชื่ออยู่แล้วไหม**
ชุดข้อมูลที่สะกดชื่อจังหวัดต่างไปนิดเดียวจะไม่พังเสียงดัง มันจะทำให้ที่อยู่
ตกไปอยู่เขตการขายที่ไม่มีอยู่จริง ตรวจแล้วตรงทุกตัวอักษร — 77 จังหวัดตรงกับ
`THAI_PROVINCES` และ 50 เขต กทม. ตรงกับ sales area master ซึ่งแปลว่า
`resolveSalesArea()` ยังทำงานกับค่าที่เลือกจาก dropdown ใหม่

ต่อ `optionsFor` เข้ากับ grid engine ด้วย — เดิม `options` เป็นลิสต์เดียวทั้งคอลัมน์
จึงทำ cascade ได้แค่ด้วยการประกาศคอลัมน์ซ้ำหนึ่งคอลัมน์ต่อค่าพ่อหนึ่งค่า
ซึ่งไหวที่ 50 เขต และไม่ไหวที่ 77 จังหวัด

**BP-3. คำเตือน "ยังไม่มีที่อยู่จัดส่ง"** — ปิดโดยเปลี่ยนกติกา ไม่ใช่ลบคำเตือน

เจ้าของโปรเจกต์ตัดสิน 2026-08-09 ว่า **ต้องมีที่อยู่จัดส่ง** จากเดิมที่สเปกบอกว่า
ไม่บังคับ คำเตือนที่ยิงคนเดียวไม่ได้จึงกลายเป็นด่านบล็อกที่ยิงได้จริง

**ครึ่งที่ทำให้มันกัดคือ `&& a.active`** เดิมด่านถามแค่ว่า "มีแถวประเภทนี้ไหม"
ขณะที่ `bpBillingAddress()` กับ `bpDeliveryAddress()` — ตัวที่ไปหาที่อยู่จริง —
ข้ามแถวที่ปิดใช้งาน คู่ค้าที่มีที่อยู่เดียวและถูกปิดไว้จึงผ่าน validation
แล้วไปคืน null ทีหลัง เป็นความผิดพลาดที่โผล่ห่างจากต้นเหตุไปหนึ่งก้าว
ตอนนี้ทั้งสองด่านถามคำถามเดียวกับตัวที่ไปหาที่อยู่

**A1c. `as unknown as` ในเส้นทางสร้างระเบียน** — ปิด 24 จาก 25 จุด
เหลือฟอร์ม Business Partner ที่ยกไปเป็นข้อ A1c-1 ข้างบน

สิ่งที่วัดได้ตอนถอด และควรจำไว้: **cast ปิดปากคอมไพเลอร์ที่ 22 จุดโดยไม่ได้ปิดบัง
อะไรเลย และปิดบังของจริงอยู่ 2 จุด** — ฟอร์มใบขอซื้อไม่เขียนสามฟิลด์ที่ A1
เพิ่งประกาศบังคับ และฟอร์มคลังสร้าง bin จากฟังก์ชันที่คืน `unknown[]`
ไม่มีทางรู้ว่าจุดไหนเป็นแบบไหนจนกว่าจะถอด ซึ่งเป็นเหตุผลว่าทำไมต้องถอดทุกจุด
ไม่ใช่เฉพาะจุดที่ดูน่าสงสัย

> `revisions` ที่ A1 บันทึกว่า "ร้ายที่สุด" — วัดจริงแล้วไม่ร้าย
> ผู้อ่านทั้งสามที่กัน `??` ไว้หมด (`(qt.revisions ??= []).push` ที่จุดเขียน ·
> `(q.revisions ?? []).length` ในแผง · `(d.revisions ?? [])` ในตัวพิมพ์)
> เดินจริงแล้วไม่พัง ไม่เงียบหาย และ snapshot ลงครบ ส่วน `approvedBy` ·
> `approvedAt` · `sentAt` ทุกที่อ่านด้วย `||` ซึ่ง `undefined` กับ `""`
> ให้ผลเหมือนกันทุกประการ **ความเสียหายอยู่ในอนาคต ไม่ใช่ปัจจุบัน** —
> คนถัดไปที่เขียน `q.revisions.length` โดยไม่กัน จะได้ TypeError
> จากฟิลด์ที่ชนิดข้อมูลรับรองว่าเป็น undefined ไม่ได้

**N-9. สามจุดใน `lib/domain/` ที่แปลงเป็น พ.ศ. ป้อนหน้าจอ** — ปิดที่ N-9a/b/c

`audit()` เขียน พ.ศ. **ลงระเบียน** ทุกครั้งที่มีคนทำอะไร ไฟล์ที่ commit สะอาด
ทริปไวร์ D5 ที่อ่านไฟล์นิ่ง ๆ จึงเขียว แต่ระเบียนเพี้ยนทันทีที่มีคนใช้งาน
แก้เป็น `stamp()` แล้ว และเพิ่มเทสต์คนละชนิด — เดินระเบียนในหน่วยความจำ
หลังเรียก workflow จริง ซึ่งเป็นชนิดเดียวที่เห็นค่าที่เกิดตอนรัน

อีกสองจุดคือป้ายปีในรายงาน แก้เป็น `ceYear` พร้อมแก้เทสต์เดิม 3 ตัว
ที่ยืนยัน `> 2500` ไว้ — แก้ได้เพราะกติกาถูกเปลี่ยนโดยตั้งใจที่ D4
ไม่ใช่ดัดให้ผ่าน และเขียนกำกับไว้ทุกตัวว่าเดิมยืนยันอะไร

ทริปไวร์ตอนนี้ครอบทั้ง repo ไม่มีไดเรกทอรีไหนถูกยกเว้น
ที่แปลงได้มีสี่ไฟล์ ระบุชื่อพร้อมเหตุผลและจำนวนบรรทัดที่อนุญาต
เพิ่มการแปลงในไฟล์ที่อนุญาตอยู่แล้วก็แดง

> ผลพลอยได้: รายการที่อนุญาตทำหน้าที่เป็นนกขมิ้นด้วย ถ้า pattern พังเมื่อไหร่
> ไฟล์ที่ควรเจอจะรายงาน 0 แล้วแดงทันที เทสต์จึงไม่เขียวเงียบ ๆ เพราะหาไม่เจอ

> เจอเพิ่มระหว่างทาง: `tests/dashboard.test.tsx` มีสำเนา heuristic ศักราช
> ของตัวเอง ที่ D2 ตามเก็บไม่ครบเพราะอยู่ในเทสต์ เทสต์ที่ถือกฎฉบับของตัวเอง
> จะตรงกับตัวเองได้ ในขณะที่ไม่ตรงกับแอป

**3. `tests/stock-card.test.tsx` flake** — แก้ 2026-08-05
เทสต์ที่ flake สองตัวคือสองตัวเดียวที่ใช้ `userEvent.type` กับช่องค้นหาของลิสต์
ทุก keystroke ที่จำลองขึ้นทำให้ตาราง 25 คอลัมน์ re-render ใหม่: ~120ms ใน jsdom
เทียบกับ **0.68ms** ที่เป็นเวลาที่แอปกรองข้อมูลจริง รหัส 8 ตัวอักษรจึงใช้เวลา ~1 วินาที
ไปกับการ re-render ล้วน ๆ ไม่เหลือ headroom ตอนรันขนาน **แอปไม่เคยช้า**
แก้โดยเปลี่ยนเป็น `fireEvent.change` หนึ่งครั้งตรงที่การพิมพ์เป็นแค่การจัดฉาก
และคง `userEvent` ไว้ตรงที่การโต้ตอบคือตัวเรื่อง ยืนยันด้วยการรันเต็มเขียวติดกันห้าครั้ง
**ถ้าชุดเทสต์อื่น flake เพราะ timeout ให้วัดเวลาตรรกะแอปแยกจากเวลา render ของ jsdom ก่อนไปแตะ timeout**

**15. `headerFromSource(...).billType` เป็นโค้ดตาย** — ลบ 2026-08-06
สาขา Sales Order คืน `billType` ที่ไม่มีใครอ่าน: ฟอร์ม invoice copy แค่
customer/PO/rep/currency/channel, record ของ invoice ไม่มีฟิลด์ `billType`
และ `effectiveBillType` หาคำตอบจาก tax rate ของบรรทัดแทน
**เหตุผลที่รอไม่ได้:** ฟิลด์ที่ไม่มีคนอ่านทำให้คนถัดไปเชื่อว่ามีกลไกที่ไม่มีอยู่จริง
ตอนนี้มีคอมเมนต์ยืนแทนที่ บอกว่าอย่าเพิ่มกลับมาโดยไม่มีคนอ่าน

**12. มีแต่ Super Admin กับ Management ที่ยืนยันคู่ค้าสถานะ Draft ได้** — ปิดที่ N1
เพิ่มบทบาท `SALES_ADMIN` ซึ่งได้ `approve` บน `business-partner` โดยตรง
(`data/admin.ts:293`) ขณะที่ Master Data ที่เหลือยังเป็น `VIEW_ONLY`
ตรงกับที่บันทึกไว้ว่าช่องว่างจริงคือ "ไม่มีบทบาทไหนตรงกับ sales admin"
ไม่ได้แก้ด้วยการเจาะรูให้ `SALES_MANAGER` ตามที่ห้ามไว้

**14. ด่านอนุมัติระดับผู้จัดการปฏิเสธใครไม่ได้เลย** — ปิดที่ N1
`SALES_ADMIN` คือบทบาทที่อนุมัติใบเสนอราคาปกติได้ แต่ถูกกั้นที่ใบซึ่งราคาต่ำกว่าพื้น
เพราะไม่ได้อยู่ใน `MANAGER_ROLES` และไม่มี flag `all` (`lib/domain/doc-draft.ts:437-444`)
มีเทสต์พิสูจน์การถูกปฏิเสธที่ `tests/sales-admin-role.test.ts`

> หมายเหตุ: ด่านนี้ "กัด" ในเชิงบทบาทแล้ว แต่จะกัดจากข้อมูลจริงก็ต่อเมื่อปิดข้อ 11 ได้ก่อน

**N-2 บางส่วน: `pickComplete` / `packComplete` ไม่มีด่านตรวจสิทธิ์เลย** — ปิดที่ N8
ตอนแก้สิทธิ์ให้ `SALES_ADMIN` เป็น `VIEW_ONLY` บน `picking`/`packing` พบว่าทั้งสองฟังก์ชัน
ไม่ได้เรียก `denied()` เลย ถ้าปล่อยไว้ การลดสิทธิ์จะเป็นแค่การซ่อนปุ่ม — ฟังก์ชันยังรันได้
ตอนนี้ `pickComplete` → `picking.edit` · `packComplete` / `packConfirmShipQty` → `packing.edit` ·
`packCreateDelivery` → `delivery-order.create` แบ่งสองฝั่งชัด และมีเทสต์ที่ทั้งสองฝั่งถูกปฏิเสธ

**6. เส้นทางขายไม่มีขั้นไหนกั้นเรื่องสต๊อก** — ปิดที่ N6
`soConfirm` อ่านสต๊อกตอนยืนยันแล้วบังคับให้ตอบสามทาง
(เปิดของค้างส่ง / ตัดจำนวน / ยกเลิกทั้งใบ) ผ่าน `askAboutShortStock`
ทาง "ตัดจำนวน" ปฏิเสธถ้าไม่ระบุผู้ยืนยันจากฝั่งลูกค้า
กฎธุรกิจถูกตัดสินแล้วว่าเป็น "จุดตัดสินใจที่บังคับตอบ" ไม่ใช่ "ปล่อยผ่านพร้อมคำเตือน"
